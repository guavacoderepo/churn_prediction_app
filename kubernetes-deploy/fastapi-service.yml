apiVersion: v1
kind: Service
metadata:
  name: fastapi-service
  namespace: churn-stack
  labels:
    app: fastapi
spec:
  selector:
    app: fastapi
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
    name: web
  type: ClusterIP
  